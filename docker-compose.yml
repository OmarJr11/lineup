services:
  db:
    image: postgres:18
    restart: always
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - pgdata:/var/lib/postgresql

  redis:
    image: redis:8
    restart: always
    volumes:
      - redis_data:/data

  api-users:
    build: .
    container_name: api-users
    command: npm run start:prod:users
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      DB_HOST: db
    depends_on:
      - db
      - redis
    restart: always

  api-admin:
    build: .
    container_name: api-admin
    command: npm run start:prod:admin
    ports:
      - "3001:3001"
    env_file:
      - .env
    environment:
      DB_HOST: db
    depends_on:
      - db
      - redis
    restart: always

  api-businesses:
    build: .
    container_name: api-businesses
    command: npm run start:prod:businesses
    ports:
      - "3002:3002"
    env_file:
      - .env
    environment:
      DB_HOST: db
    depends_on:
      - db
      - redis
    restart: always

  migrate:
    build: .
    command: npm run migration:run
    env_file:
      - .env
    environment:
      DB_HOST: db
    depends_on:
      - db

volumes:
  pgdata:
  redis_data:
